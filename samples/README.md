# Samples

No sample applications yet. A console or Blazor probe app will be added once v1 has a real HTTP surface, following the same “sample reflects implemented SDK only” rule as freeagent-dotnet.
